export function convertBudgetProposalToDynamics(userId: string, organizationId: string, expenseReportId: string, e: any): any {
  console.log('No conversion of budget proposal yet.', e);
  return null;
}
